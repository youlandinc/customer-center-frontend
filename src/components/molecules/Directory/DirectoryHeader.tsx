import {FC} from 'react';

import {StyledCreateContact} from '@/components/atoms';

export const DirectoryHeader: FC = (props) => {
    const {} = props;

    return (
        <div>
            <StyledCreateContact/>
        </div>
    )
};